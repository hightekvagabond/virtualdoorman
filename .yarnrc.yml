# React Native, Metro and Gradle all expect a real node_modules tree on disk,
# so we opt out of Plug'n'Play. No zero-installs either: the Yarn cache and
# install state stay out of git (see .gitignore).
nodeLinker: node-modules

enableGlobalCache: true
